{
  "title": "Momcozy Bridge",
  "config": {
    "step": {
      "user": {
        "title": "Connect a Momcozy camera",
        "description": "Enter the host, port, stream path and RTSP credentials from your bridge setup.",
        "data": {
          "name": "Name",
          "host": "Bridge host",
          "port": "RTSP port",
          "path": "Stream path",
          "username": "RTSP username",
          "password": "RTSP password"
        }
      },
      "reconfigure": {
        "title": "Connect a Momcozy camera",
        "description": "Enter the host, port, stream path and RTSP credentials from your bridge setup.",
        "data": {
          "name": "Name",
          "host": "Bridge host",
          "port": "RTSP port",
          "path": "Stream path",
          "username": "RTSP username",
          "password": "RTSP password"
        }
      }
    },
    "error": {
      "cannot_connect": "Connection failed. Check the bridge, host, firewall, port and path.",
      "invalid_auth": "Check the RTSP username and password.",
      "invalid_config": "Check host, port, path and name. Enter both RTSP credentials or leave both empty."
    },
    "abort": {
      "already_configured": "This bridge endpoint is already configured.",
      "already_in_progress": "This endpoint is already being configured.",
      "reconfigure_successful": "Connection settings updated."
    }
  }
}
